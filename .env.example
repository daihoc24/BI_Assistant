# Không điền API key thật vào file này.
# Khi chạy local, copy thành .env.local rồi điền giá trị thật nếu muốn bật AI provider.

OPENAI_API_KEY=
OPENAI_MODEL=gpt-4.1-mini
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_API_MODE=chat
